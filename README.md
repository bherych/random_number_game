# Random Number Game

A simple game that generates random number and makes you guess it.

The algorithm accepts three arguments: the lowest possible number, the highest possible number, and number of retries.
